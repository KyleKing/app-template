<h1 class="c-main__title">Hello {{ name ?? "World" }}!</h1>
<p class="c-main__content">
  The path segment is echoed straight into this heading. Vento autoescapes it, so the nav's "Alert" link renders a
  script tag as text instead of running it.
</p>
